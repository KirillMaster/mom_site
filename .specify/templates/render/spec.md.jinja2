# Feature Specification: {{ feature }}

**Branch**: `{{ branch }}` | **Created**: {{ created }} | **Status**: {{ status }}

**Input**: {{ input }}

## User Scenarios & Testing

{% for story in user_stories %}
### User Story {{ loop.index }} - {{ story.title }} (Priority: {{ story.priority }})

{{ story.description }}

**Why this priority**: {{ story.why_priority }}

**Motivation**:
- **Problem**: {{ story.motivation.problem }}
- **Value**: {{ story.motivation.value }}
- **Consequence if skipped**: {{ story.motivation.consequence_if_skipped }}

**Independent Test**: {{ story.independent_test }}

**Acceptance Scenarios**:

{% for scenario in story.acceptance_scenarios %}
{{ loop.index }}. **Given** {{ scenario.given }}, **When** {{ scenario.when }}, **Then** {{ scenario.then }}
{% endfor %}

**Acceptance Criteria**:

{% for ac in story.acceptance_criteria %}
- [{{ 'x' if ac.done else ' ' }}] `{{ ac.id }}` {{ ac.description }}
{% endfor %}

**Test Scenarios**:

{% for ts in story.test_scenarios %}
- `{{ ts.id }}` ({{ ts.level }}{% if ts.automation_ref %}, `{{ ts.automation_ref }}`{% endif %})
  - Given: {{ ts.given | join('; ') }}
  - When: {{ ts.when }}
  - Then: {{ ts.then | join('; ') }}
  - Verification: {{ ts.verification | join(', ') }}
{% endfor %}

{% endfor %}

## Edge Cases

{% for ec in edge_cases %}
- `{{ ec.id }}` {{ ec.description }} → {{ ec.expected_behavior }}
{% endfor %}

## Functional Requirements

{% for fr in functional_requirements %}
- **{{ fr.id }}**: {{ fr.text }} (stories: {{ fr.story_refs | join(', ') }})
{% endfor %}

## Key Entities

{% for entity in key_entities %}
- **{{ entity.name }}** (`{{ entity.id }}`): {{ entity.description }}
{% endfor %}

## Success Criteria

{% for sc in success_criteria %}
- **{{ sc.id }}**: {{ sc.description }} (measured via: {{ sc.measurable_via }})
{% endfor %}

## Assumptions

{% for a in assumptions %}
- `{{ a.id }}` {{ a.description }}
{% endfor %}
