# QA report — 002 artwork-pages / Slice S2

Coder commit under review: `6adb7a2` on `bob/002-artwork-pages/S2`.

## Artifact integrity
- `.bob/features/artwork-pages.feature` and `.bob/features/artwork-pages.qa.md` diff byte-identical against `.bob/staging/002/features/` — no tampering.

## Scenario results (procedures executed literally via Jest, mirroring the approved `.qa.md`; a live dev server was not started, so curl steps were run as equivalent React Testing Library renders of `ArtworkPage`/`GalleryClientPage`, which is what the coder's own committed tests do)

| id | verdict | evidence |
|---|---|---|
| S2-AS1 | pass | `GalleryClientPage.test.tsx` test `@S2-AS1` — card anchor `href="/gallery/osenniy-sad-7"` for artwork id 7 "Осенний сад". Confirmed in source: `getArtworkHref` wraps the image block in `<a href="/gallery/<slug>">`. |
| S2-AS2 | pass | `page.test.tsx` `@S2-AS2` — renders `45 000 ₽`-style text and a "Узнать цену" link for isForSale=true/price=45000. |
| S2-AS3 | pass | `@S2-AS3` — `isForSale=true, price=null` renders literal "цена по запросу" plus the CTA link. |
| S2-AS4 | pass | `@S2-AS4` — `isForSale=false` renders no "Узнать цену" link. |
| S2-AS5 | pass | `@S2-AS5` — CTA href matches `/^\/contacts\?/` and contains id `7`; click fires `reachGoal(Goals.ContactClick, {channel: 'ask_price', ...})` via the `AskPriceButton` client component. |
| S2-AS6 | pass | `@S2-AS6` — related-works section titled "Другие работы этой категории" lists a same-category sibling and excludes the artwork itself. |
| S2-AS7 | pass | `@S2-AS7` — sole-in-category artwork renders no "Другие работы этой категории" section at all. |
| S2-AS8 | pass | `@S2-AS8` — breadcrumb nav order `Главная` (`href="/"`) → `Галерея` (`href="/gallery"`) → artwork title (plain text, not a link). |
| S2-AS9 | pass | `@S2-AS9` — `<img>` src equals `imagePath` (`/full/osenniy-sad.jpg`), not `thumbnailPath`; page text includes description "Осенний пейзаж маслом" and category "Пейзажи". |

All 9 S2 scenarios: **pass**.

## Traceability
Scenario ids in feature file for this slice: S2-AS1..S2-AS9 (9).
Traced test ids found (`grep -rn '@S2-AS' frontend/app/gallery`):
- `@S2-AS1` — GalleryClientPage.test.tsx
- `@S2-AS2`..`@S2-AS9` — `app/gallery/[slug]/page.test.tsx`

All 9 covered 1:1, no orphan/missing ids.

## Full regression run
- `cd frontend && npx jest --ci --passWithNoTests --testPathIgnorePatterns='e2e/'` → **16 suites / 92 tests passed**, exit 0. Matches coder's reported numbers.
- `cd frontend && npx tsc --noEmit` → exit 0, no errors.
- `npx playwright test` for S2 was not executed (no e2e spec added for S2 in this slice's diff, and no dev server available in this environment) — the jest-level DOM tests above cover the same click/navigation assertions the procedure describes for S2-AS5.

## Risk checks (facts, not assumptions)

1. **Lightbox vs card-link separation.** `GalleryClientPage.tsx`: the eye button's `onClick` is `handleOpenLightbox`, which calls `event.preventDefault(); event.stopPropagation();` before `openLightbox(index)`. The button sits inside the new `<a href={getArtworkHref(artwork)}>` wrapper. Clicking the eye button therefore opens the lightbox and does not navigate; clicking anywhere else on the image area follows the anchor to `/gallery/<slug>`. Confirmed by reading the JSX structure directly (button nested inside the anchor, preventDefault/stopPropagation present). No test explicitly asserts non-navigation-on-eye-click in this diff, but the code path is unambiguous.
2. **`reachGoal(Goals.ArtworkView, ...)` on lightbox open.** Present, unchanged: `openLightbox` still calls `reachGoal(Goals.ArtworkView, { id: artwork?.id, title: artwork?.title })`. Not touched by this slice's diff.
3. **Existing GalleryClientPage tests not weakened/removed.** `git diff main...HEAD -- frontend/app/gallery/GalleryClientPage.test.tsx` shows a pure addition (+7 lines, one new `it()` block for `@S2-AS1`); nothing removed. File grew 140 → 147 lines. Price/CTA/aspect-square/category-filter/exhibition-hide tests are untouched.
4. **No price CTA for exhibition photos on the artwork page.** `showPriceCta = artwork.isForSale && !isExhibitionPhoto(artwork, categories)` in `page.tsx`; covered by `@S1-AS9` test (exhibition photo hides "Узнать цену").
5. **Server/client component split.** `page.tsx` has no `'use client'` directive (first lines are plain imports) — remains a server component. `AskPriceButton.tsx` starts with `'use client';` on line 1. Correct split.
6. **Slug consistency between card link and page resolution.** Both `GalleryClientPage.tsx` (`getArtworkHref`) and `page.tsx`/related-works list use the same `buildArtworkSlug(title, id)` from `lib/artworkSlug.ts`; `resolveArtworkBySlug` on the page extracts the trailing id and ignores the title text, so any slug built by `buildArtworkSlug` for a real artwork id always resolves — no 404 risk from title drift.
7. **"Мусорный файл" claim / nothing significant deleted.** `git diff --stat main...HEAD` and `git show --stat 6adb7a2` show only: `.bob/features/*` (2 new files, expected — approved feature/QA docs added to the slice), `GalleryClientPage.tsx/.test.tsx`, `[slug]/AskPriceButton.tsx` (new), `[slug]/page.tsx`, `[slug]/page.test.tsx`. Every line is additive except a small refactor inside `GalleryClientPage.tsx` (wrapping the image block in an anchor) — no file deletions in the commit, no unexplained removals anywhere in the diff.
8. **`generateMetadata`, schema.org, `middleware.ts` untouched.** `grep` for `generateMetadata`/`application/ld+json`/`schema.org` in `page.tsx` returns nothing (feature not implemented in this slice, as expected — that's S3). `git diff main...HEAD` touches no `middleware.ts` file at all (not in the changed-files list).

No risks materialized; nothing found to send back.

## Verdict
**ok** — all 9 S2 scenarios pass, traceability complete, regression suite green (16/92), tsc clean, all five named risks verified safe by direct code/diff inspection.
